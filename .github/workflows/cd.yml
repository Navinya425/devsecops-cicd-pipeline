name: CD Pipeline

on:
  push:
    branches:
      - main
      - automated-cd-deployment

jobs:

  docker-build-push:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            navinya425/devsecops-app:latest
            navinya425/devsecops-app:${{ github.sha }}

  deploy-dev:

    needs: docker-build-push

    runs-on: self-hosted

    environment: dev

    steps:

      - name: Pull Latest Docker Image
        run: docker pull navinya425/devsecops-app:latest

      - name: Remove Old DEV Container
        shell: powershell
        continue-on-error: true
        run: |
          docker rm -f dev-app

      - name: Deploy DEV Container
        shell: powershell
        run: |
          docker run -d -p 5001:5000 `
          -e APP_ENV=development `
          --name dev-app `
          navinya425/devsecops-app:latest

  deploy-staging:

    needs: deploy-dev

    runs-on: self-hosted

    environment: staging

    steps:

      - name: Pull Latest Docker Image
        run: docker pull navinya425/devsecops-app:latest

      - name: Remove Old STAGING Container
        shell: powershell
        continue-on-error: true
        run: |
          docker rm -f staging-app

      - name: Deploy STAGING Container
        shell: powershell
        run: |
          docker run -d -p 5002:5000 `
          -e APP_ENV=staging `
          --name staging-app `
          navinya425/devsecops-app:latest

  deploy-production:

    needs: deploy-staging

    runs-on: self-hosted

    environment: production

    steps:

      - name: Pull Latest Docker Image
        run: docker pull navinya425/devsecops-app:latest

      - name: Remove Old PROD Container
        shell: powershell
        continue-on-error: true
        run: |
          docker rm -f prod-app

      - name: Deploy PROD Container
        shell: powershell
        run: |
          docker run -d -p 5003:5000 `
          -e APP_ENV=production `
          --name prod-app `
          navinya425/devsecops-app:latest